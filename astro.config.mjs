// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Canonical site URL for GitHub Pages (user/org page served at the domain root).
  // Used to build absolute URLs in sitemaps, canonical tags, etc.
  site: 'https://ajarteag.github.io',
});
